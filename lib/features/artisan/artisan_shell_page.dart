import 'package:flutter/material.dart';

class ArtisanShellPage extends StatelessWidget {
  const ArtisanShellPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Artisan Dashboard (placeholder)')),
      body: const Center(child: Text('Artisan area — coming soon')), 
    );
  }
}
